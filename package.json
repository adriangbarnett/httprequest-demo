{
  "name": "httprequest-demo",
  "version": "1.0.0",
  "description": "Create a HTTP request, send the requestion, wat for the respone, then return some data back to the controller.",
  "main": "app.js",
  "dependencies": {
    "express": "^4.18.1",
    "request": "^2.88.2"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "app": "nodemon app.js",
    "db": "nodemon db.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
